# Paper 1 — Substrate-Agnostic Nerve Protocol

**Status:** 15 pages, 536 KB, 24/24 references resolved, 0 undefined citations. Last built at master commit `5e43fd8` (2026-05-20).

## Build Instructions

### Pre-build: Regenerate figures

Cycle trace figure must be regenerated before compiling:

```bash
cd /Users/electron/Documents/Projets/nerve-wml-wt-gap
uv run --with matplotlib python -c \
  "from scripts.render_paper_figures import render_cycle_trace; render_cycle_trace()"
```

### Compile

From this directory (`papers/paper1/`):

```bash
pdflatex main.tex && bibtex main && pdflatex main.tex && pdflatex main.tex
```

Or using Tectonic (single command, slower):

```bash
tectonic main.tex
```

## Paper Structure

- **Main text** (§1–§7): 15 pages
  - Introduction & related work (§1–§2)
  - Method & protocol specification (§3–§4)
  - Empirical results (§5–§6)
  - Discussion (§7)
- **Limitations** (§8): 10 untested scopes + 2 mitigation arguments (γ/θ empirical stress, carrier-spectrum metric)
- **References**: 24 fully resolved citations

## Supplementary Material

Empirical replication of Gröger, Wen & Brbić (2026) Prop 4.2 (N-dependent null calibration) located at:

```
papers/paper1/supplementary/cknna-n-dependence-replication/
```

Data source: `docs/superpowers/research/2026-05-20-macm1-scientific-eval.json`  
See supplementary README for figure regeneration and build instructions.

## Figures

Artefacts in `figures/` are regenerated by `scripts/render_paper_figures.py` and should not be hand-edited:

- `w2_hard_scaling.pdf` — protocol scaling (regenerated from JSON)
- `info_transmission.pdf` — MI results (regenerated from JSON)
- `cycle_trace.pdf` — **CRITICAL** (requires pre-build regen, see above)

## arXiv Submission

See `docs/ARXIV_SUBMISSION.md` for pre-submission checklist and metadata template.
